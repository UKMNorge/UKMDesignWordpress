<ol class="list-unstyled">
    {% for arrangement in arrangementer|reverse|filter(arrangement => arrangement.erRegistrert() and arrangement.erSynlig() and arrangement.getSesong() > 2019 ) %}
        <li class="clearfix mb-3" style="{{ arrangement.erFerdig() ? 'opacity: .65' }}">
            {% include "Arrangement/ListeElement.html.twig" %}
        </li>
    {% else %}
        <li>{{ omrade.getNavn() }} har ingen arrangement.</li>
    {% endfor %}
</ol>