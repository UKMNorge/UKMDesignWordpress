{% extends "UKMDesign/Layout/base.html.twig"|UKMpath %}

{% block content %}
{% set omradeArr = omrader ? omrader : [omrade] %}

	<div class="container">
	{% for omrade in omradeArr %}
		<div class="row">
			<div class="col-12">
				<h3>Kontaktpersoner for
					{{ omrade.getNavn() }}</h3>
			</div>
			{% for kontakt in omrade.getKontaktpersoner().getAll() %}
				<div class="col-12 col-sm-6 col-lg-4 col-xl-3">
					{% embed "Kontaktpersoner/Grid.html.twig"|UKMpath %}{% endembed %}
				</div>
			{% else %}
				<div class="col-12">
					<p>
						Vi har ikke kontaktinformasjon for
						{{ omrade.getNavn() }}
						enda.
						Vi jobber med å skaffe det, så du må gjerne prøve igjen om noen dager.
					</p>
				</div>
			{% endfor %}
		</div>
		{% endfor %}

		<div class="clearfix my-3"></div>
		<div class="row">
			<div class="col-12">
				<p>
					{% if omrader %}
						<a href="{{ omrade.getLink() }}">Tilbake</a>
					{% else %}
						<a href="{{ omrade.getLink() }}">Tilbake til
							{{ omrade.getNavn() }}</a>
					{% endif %}
				</p>
			</div>
		</div>
	</div>
{% endblock %}
